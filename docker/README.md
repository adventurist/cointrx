To start fresh, wait for the the applications to become active and then run the following commands:

curl http://localhost:6969/regtest/key/clear-all
curl http://localhost:6969/regtest/address/provision-all
